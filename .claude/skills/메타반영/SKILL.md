---
name: 메타반영
description: 기획자가 채운 결과 문구 표(docs/용신_결과문구_작성양식 — Numbers .numbers 또는 Excel .xlsx)를 읽어 부적집 결과 화면 문구에 반영한다. "메타반영", "문구 반영해줘", "넘버스 반영", "엑셀 반영", "기획 문구 넣어줘", "결과 문구 업데이트" 라고 하면 실행.
---

# 결과 문구 표 → 코드 반영

기획자가 작성한 문구 표(Numbers 또는 Excel)를 `web/src/phrases.js`에 반영하고, 배포 파일까지 새로 만든다.
**사용자는 개발자가 아닐 수 있다.** 명령어를 늘어놓지 말고, 알아서 실행한 뒤 결과만 쉬운 말로 알려줄 것.

## 순서

### 1. 파일 위치 확인

기본은 `docs/용신_결과문구_작성양식.numbers`(있으면), 없으면 `docs/용신_결과문구_작성양식.xlsx`.
기획자는 맥의 **Numbers**로 작업하므로 보통 `.numbers`가 최신본이다.
`apply-phrases.py`가 `.numbers`를 감지하면 **Numbers 앱을 시켜 자동으로 xlsx로 변환한 뒤** 읽는다
(반영 중 Numbers 창이 잠깐 떴다 닫힌다 — 정상이다. 맥에서만 되고, 처음이라면 자동화 권한 허용 창이 한 번 뜰 수 있다).
사용자가 다른 파일(다운로드 폴더 등)을 언급하거나 첨부했다면 그 경로를 쓴다.
경로가 애매하면 추측하지 말고 어느 파일인지 한 번 묻는다.

### 2. 반영 실행

```bash
python3 tools/apply-phrases.py [파일경로]
```

경로를 생략하면 위 기본 파일(`.numbers` 우선)을 알아서 찾는다. `.numbers`·`.xlsx` 둘 다 인자로 넘길 수 있다.

- 안 채워진 칸이 있으면 **아무것도 쓰지 않고** 어느 시트 몇 번 칸이 비었는지 알려준다.
  그대로 사용자에게 전달하고 여기서 멈춘다. 빈 칸을 임의로 채우지 말 것.
- `⚠` 경고(예: 예시 문구 그대로, E 시트 일부만 작성)는 반영은 되지만 반드시 사용자에게 전달한다.

### 3. 배포 파일 다시 만들기

```bash
cd web && npm run build
```

`web/dist/index.html` 한 파일이 실제 배포물이다.

### 4. 실제로 바뀌었는지 확인

프리뷰를 띄우고 회귀 케이스로 눈으로 확인한다.

- 프리뷰: `.claude/launch.json`의 `bujeokjip` (preview_start) → `http://localhost:8017/web/dist/`
- 확인 입력: **1979년 2월 2일 16시 45분 · 서울**
- 기대: 사주팔자 `무오 을축 경자 갑신`, 용신 한자 `火`
- 결과 화면에 새 문구가 나오는지, 용신 설명 영역(있다면)이 보이는지 확인
- 콘솔 에러가 없어야 한다

### 5. 보고

바뀐 문구를 **before → after로 요약**해서 보여준다. 전체를 다 붙여넣지 말고 달라진 것만.
커밋·푸시는 사용자가 요청할 때만 한다.

## 알아둘 것

- `web/src/phrases.js`는 **자동 생성 파일**이다. 직접 고치지 말 것 — 다음 반영 때 덮어써진다.
  문구를 바꾸려면 Numbers(또는 Excel) 표를 고치고 이 명령을 다시 돌린다.
- 문구 안의 `{용신}` `{기신}` `{희신}` 은 사람마다 다른 오행 이름으로 자동 치환되는 변수다. 그대로 둔다.
- 조후(C) 블록은 사주가 춥거나 더운 사람에게만 나간다. 평범한 사람은 이 문장이 빠진 채 조립되므로,
  C 없이도 말이 되는지 확인 대상이다.
- 용신 설명(E 시트)이 비어 있으면 그 영역은 화면에 안 나온다. 오류가 아니다.
- 표 자체(칸 구조·안내문)를 고쳐달라는 요청이면 이 명령이 아니라 원본 파일(`docs/용신_결과문구_작성양식.numbers` 또는 `.xlsx`)을 직접 편집한다.
- **Numbers 변환은 macOS 전용**이다. 맥이 아니거나 Numbers가 없으면 스크립트가 그 사실을 알려주며 멈춘다 — 그때는 Numbers에서 'Excel로 내보내기' 한 xlsx로 다시 돌리면 된다.
