# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트 개요

사주명리 용신(用神) 산출 엔진과 이를 쓰는 단일 파일 제품.

- `policy/용신산출_정책명세서_v.0.1.md` — 하드코딩 로직을 추출한 개발 인계용 정책·산식 명세서. **정책의 소스 오브 트루스.** 로직 질문은 코드보다 이 문서를 먼저 볼 것.
- `yongsin.html` — 엔진 원본 프로토타입(순수 HTML/CSS/JS, 의존성 없음). 계산 로직 전량이 인라인 `<script>`에 있다.
- `부적집-ver20260727.html` — **레거시 프로토타입(동결).** 한때 제품이었으나 `web/`이 대체했다. 디자인은 피그마 "부적집 프로토타입"(비밀번호 보호) 기준. 엔진 함수는 yongsin.html과 동일 로직의 인라인 복사본이라 구 시각 정책·수성공식 근사에 머물러 있고, 시각 입력이 12지시 드롭다운이며 태어난 도시는 계산에 반영되지 않는다. **현행 동작의 근거로 삼지 말 것** — 아래 '로직 동기화' 표 참조.
- `policy/2026-07-27.md` — 작업 인계 로그. 남은 TODO(기획 문구 13종 교체, 캘리그래피 이미지 4종, 구매안내 샘플 사진, QR, 웹 게시)는 이 파일이 기준.
- `policy/용신_결과문구_작성양식.xlsx` + `tools/apply-phrases.py` + `.claude/skills/메타반영/` — **기획 문구 반영 파이프라인.** 기획자가 엑셀 노란 칸(13개 문구 블록 + 용신 설명 5종)을 채우면 `메타반영` 커맨드가 `web/src/phrases.js`를 자동 생성한다. **`phrases.js`는 자동 생성 파일이므로 직접 수정 금지** — 다음 반영 때 덮어써진다. 스크립트는 Python 표준 라이브러리만 사용(친구 PC에서 pip 설치 불필요).
- `web/` — **현행 제품 화면.** `engine/` 모듈을 esbuild로 번들해 단일 파일로 배포(`cd web && npm run build`). 빌드는 **`web/dist/index.html` 과 저장소 루트 `docs/index.html` 두 곳에 같이 쓴다** — `docs/`가 GitHub Pages 게시 경로(Settings → Pages → main `/docs`)라 수동 복사하면 옛 화면이 게시되기 때문. `docs/index.html`은 자동 생성물이므로 직접 수정 금지. 브라우저는 KASI·NASA Horizons 둘 다 CORS로 막히므로 네트워크 0 모드(일진 JDN + **NASA 절기 시각표 내장**)로 동작. 로컬 확인은 `/서버실행` 커맨드 또는 `.claude/launch.json`의 `bujeokjip`(node web/serve.mjs, 8017).
- `engine/` — Node(≥18) 재구현. 1단계 만세력 완료: 일진=KASI 음양력 API(폴백 JDN+49), 절기=KASI 특일 API(2000~2028만 제공)→천문 계산(Meeus, 분 단위)→수성공식 순. KASI `lunSecha`·`lunWolgeon`은 음력 기준이라 사주 연·월주에 쓰면 안 되고, KASI 절기 이상치(2011 입동)는 천문값으로 자동 보정. 검증: sxtwl 교차 18,963건(1회 수행, 스크립트는 초기 커밋 이력에만 있음)·NASA JPL Horizons(DE441) 대조 96건 최대 8분 통과 — 기록·재실행법은 engine/README.md. 테스트 `cd engine && npm test`.

주요 명령:

| 목적 | 명령 |
|---|---|
| 제품 빌드 (dist + docs 게시본) | `cd web && npm run build` |
| 엔진 회귀 테스트 | `cd engine && npm test` |
| 로컬 확인 | `/서버실행` 커맨드 (빌드 → 서버 → 브라우저), 수동은 `cd web && npm run serve` (8017) |
| 기획 문구 반영 | `/메타반영` 커맨드 (내부적으로 `python3 tools/apply-phrases.py`) |

린트는 없다. 레거시 HTML 2종은 브라우저로 직접 열면 실행된다. 검증은 아래 회귀 케이스.

## 계산 파이프라인

입력(생년월일시·성별) → ① 만세력 원국 4기둥(`computeChart`) → ② 오행·십신 분포 → ③ 강약/억부 점수(`strength`) → ④ 한난조습/조후 점수(`climate`) → ⑤ 용신 결합 판정(`yongsin`) → ⑥ 대운(`daewoon`) → ⑦ 해석 문구 렌더(`render`)

핵심 산출물: 용신 오행 1개 + 파생 4신(희신·기신·한신·구신).

## 절대 불변식

- **인덱스 체계 고정** (명세서 §1): 천간 0~9(甲~癸), 지지 0~11(子~亥), 오행 0~4(목화토금수). 모든 산식이 이 순서 전제의 모듈러 연산 — 상생 `(a+1)%5===b`, 상극 `(a+2)%5===b`. 순서 변경 금지.
- **일주 오프셋 `+49`** (§3-1): JDN→60갑자 변환의 기준 상수. 건드리면 알려진 사주로 대조 검증 필수.
- **억부 우선 원칙** (§6-2, 핵심 정책 — 2026-08-14 개정, 초판은 조후 우선이었음): 억부 후보가 있으면 **억부가 용신**(`abu[0]`), 조후는 참고로 밀린다. 단 조후 오행이 억부 후보 안에 있으면 그것을 골라 `both=true`. 억부 후보가 없을 때(중화)만 조후가 용신.
- **§6 정책 3건 확정(2026-08-14, 명세서 §6-2-1)**: SAZU 대조에서 갈린 ①억부 방향(중화 시 후보 비움) ②억부 후보 순위(신강=관성 우선) ③조후 결합(조후가 후보 순위 재배열) — **전부 현행 유지**로 결정. 유파 차이지 오류가 아니다. 회귀 테스트 `§6-1/§6-2 … 고정`이 지키므로 임의로 바꾸지 말 것.
- **정책 수치는 기획 소유** (§4~6): 배점·임계값·우선순위는 명리 휴리스틱. 도메인 전문가(기획) 승인 없이 변경 금지.
- **해석 문구 AI 생성 금지** (§11): 결과 문구 블록(A용신×5, B강약×5, C조후 cold/hot/mild, E키워드, F한줄소개)을 기획이 전량 작성하고 코드는 조립만 한다. A·B·C는 키마다 문구 여러 개를 두고 화면에서 랜덤 1개, E 키워드는 랜덤 7개(2026-08-18). C `mild` 후보가 없으면 평(平)일 때 C 생략. D(부적처방)는 화면에서 제외. 블록은 넘버스→`web/src/phrases.js` 자동 생성(하드코딩 금지, `/메타반영`).

## 회귀 테스트 기준 (§9)

입력: 1979-02-02(양력) 16:45, 여자, 서울(경도 보정 ON → 진태양시 16:13)

기대: 사주년 1978 / 원국 무오·을축·경자·갑신 / 일간 경금 / 강약 +1(중화 신강쪽) / 조후 net −2(한, need=화) / **용신 火 (both=true)** / 희신 木 · 기신 水

로직을 수정하면 이 케이스가 유지되는지 반드시 확인한다. yongsin.html 첫 진입 시 이 값이 기본 입력으로 자동 산출된다.

`web/` 동일 케이스: 1979년 2월 2일 16시 45분 · 서울 → 사주팔자 "무오 을축 경자 갑신", 결과 문구에 "물의 기운이 너무나 강한" + "게다가 사주가 차게" 포함, 용신 한자 火, 계산 근거에 "진태양시 16:13".

`web/` 시각 경계 케이스: 같은 날 23:30 → 진태양시 22:58이라 아직 해시(일주 경자·정해시) / 23:45 → 진태양시 23:13으로 자시(일주 신축·무자시) / 1988-08-15 12:00 → 서머타임 되돌림(진태양시 10:28).

## 알려진 한계·확정된 처리 정책 (§3-3, §10)

- 절기: engine은 정밀 절기를 쓴다. **제품(`web/`)은 `termsProvider: 'nasa'`** — NASA JPL Horizons(DE441) 절입 시각을 빌드 시점에 받아 `engine/src/nasa-terms-data.js`로 구워 넣고 런타임엔 표만 읽는다(네트워크 0, 오차 0). 표 범위(1899~2030) 밖 연도만 Meeus 천문 계산 폴백. Node 쪽 기본은 여전히 `'kasi'`(KASI 특일 → 천문 계산). 레거시 HTML 2종은 수성공식 근사(±1일).
  - **±8분은 Meeus 천문 계산의 허용 오차**(§3-3)였고, 1899~2030 전수 대조에서 실제 최대 편차는 **14분**(2013 입하)으로 그 상한을 넘었다. 내장 표를 쓰는 제품 경로에는 해당하지 않는다(오차 0).
  - 표 재생성: `node engine/tools/fetch-nasa-terms.mjs 1899 2030` (Horizons 순차 호출, 6~15분). 연도 범위를 넓히려면 이 명령을 다시 돌린다.
- **시각 정책 확정(2026-08-14, §3-6)**: 경도 보정 기본 ON(서울 −32분) · 서머타임 적용(1948~60, 1987~88) · 표준시 이력 적용(UTC+8:30 시행기) · **자시 시작·일주 경계 = 진태양시 23:00**(`manse.js`의 `ZI_BOUNDARY`. 시지 경계와 동일해 야자시 구간 없음. 서울 KST 23:32 — 관습 "자시 23:30"은 경도 보정의 근사치라 둘을 같이 쓰면 이중 적용, SAZU 대조로 확인). 단 **절기 비교에는 이 보정을 적용하지 않는다** — 절입은 물리적 순간이라 순간 대 순간 비교. 음력 입력 미지원.
- 출생 시각은 필수 입력(없으면 진행 차단 모달), 날짜 오류는 드롭다운 제약으로 원천 차단, 실제 예외 폴백은 전역 에러 페이지 하나뿐(§10 A~E 분류 참조).

## 로직 동기화

엔진(§1~§6) 구현은 네 곳에 있고, **현행은 `engine/` 하나뿐**이다.

| 위치 | 상태 |
|---|---|
| `policy/용신산출_정책명세서_v.0.1.md` | 소스 오브 트루스. 로직 변경 시 반드시 함께 갱신 |
| `engine/` + `web/` | **현행 구현.** 정밀 절기 · §3-6 개정 시각 정책 반영 |
| `yongsin.html` | 레거시 프로토타입. 수성공식 근사 · 구 시각 정책(자정 기준·보정 OFF)에서 **동결** |
| `부적집-ver20260727.html` | 레거시 프로토타입. 같은 사유로 동결 — `web/`이 대체 |

레거시 2종은 개정 정책을 반영하지 않았다(경계 케이스에서 engine과 다른 결과가 나올 수 있음). 폐기할지 갱신할지는 기획 결정 사항.

알려진 불일치 1건: 결과 문구 조립 순서가 부적집은 피그마 기준 B→A→C→D, 명세서 §11-3은 A→B→C→D. 기획 최종안 확정 시 명세서를 갱신하기로 함(policy/2026-07-27.md §4).
