# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트 개요

사주명리 용신(用神) 산출 엔진과 이를 쓰는 단일 파일 제품.

- `docs/용신산출_정책명세서_v.0.1.md` — 하드코딩 로직을 추출한 개발 인계용 정책·산식 명세서. **정책의 소스 오브 트루스.** 로직 질문은 코드보다 이 문서를 먼저 볼 것.
- `yongsin.html` — 엔진 원본 프로토타입(순수 HTML/CSS/JS, 의존성 없음). 계산 로직 전량이 인라인 `<script>`에 있다.
- `부적집-ver20260727.html` — **실제 제품**(웹 게시 + QR 배포 예정). 최신 `부적집-ver*.html`이 현행본이다. 디자인은 피그마 "부적집 프로토타입"(비밀번호 보호) 기준. v1은 용신찾기 플로우만 구현(60간지찾기는 준비중 처리). 엔진 함수는 yongsin.html과 동일 로직의 인라인 복사본, 결과 문구는 §11 블록 조립(`PHRASES` 상수 — draft, 기획 감수 필요). 시각 입력은 12지시 드롭다운(시진 인덱스=시지, 자시는 선택 날짜 기준 조자시 취급), 태어난 도시는 UI만 있고 계산 미반영. 유일한 외부 의존은 구글 웹폰트(오프라인이면 시스템 명조 폴백).
- `docs/2026-07-27.md` — 작업 인계 로그. 남은 TODO(기획 문구 13종 교체, 캘리그래피 이미지 4종, 구매안내 샘플 사진, QR, 웹 게시)는 이 파일이 기준.
- `engine/` — Node(≥18) 재구현. 1단계 만세력 완료: 일진=KASI 음양력 API(폴백 JDN+49), 절기=KASI 특일 API(2000~2028만 제공)→천문 계산(Meeus, 분 단위)→수성공식 순. KASI `lunSecha`·`lunWolgeon`은 음력 기준이라 사주 연·월주에 쓰면 안 되고, KASI 절기 이상치(2011 입동)는 천문값으로 자동 보정. sxtwl 교차 검증 18,963건 실질 불일치 0, NASA JPL Horizons(DE441) 대조 96건 최대 8분. 상세는 engine/README.md, 테스트 `cd engine && npm test`.

빌드·린트·테스트 명령 없음. 브라우저로 HTML을 직접 열면 실행되고, Claude Code에서는 `.claude/launch.json`의 `bujeokjip` 프리뷰 서버(python3 http.server, 포트 8017)로 확인한다. 검증은 아래 회귀 케이스 수동 확인.

## 계산 파이프라인

입력(생년월일시·성별) → ① 만세력 원국 4기둥(`computeChart`) → ② 오행·십신 분포 → ③ 강약/억부 점수(`strength`) → ④ 한난조습/조후 점수(`climate`) → ⑤ 용신 결합 판정(`yongsin`) → ⑥ 대운(`daewoon`) → ⑦ 해석 문구 렌더(`render`)

핵심 산출물: 용신 오행 1개 + 파생 4신(희신·기신·한신·구신).

## 절대 불변식

- **인덱스 체계 고정** (명세서 §1): 천간 0~9(甲~癸), 지지 0~11(子~亥), 오행 0~4(목화토금수). 모든 산식이 이 순서 전제의 모듈러 연산 — 상생 `(a+1)%5===b`, 상극 `(a+2)%5===b`. 순서 변경 금지.
- **일주 오프셋 `+49`** (§3-1): JDN→60갑자 변환의 기준 상수. 건드리면 알려진 사주로 대조 검증 필수.
- **조후 우선 원칙** (§6-2, 핵심 정책): 억부 후보와 조후 필요 오행이 다르면 **조후가 용신**. 억부 1순위는 보조로 밀린다.
- **정책 수치는 기획 소유** (§4~6): 배점·임계값·우선순위는 명리 휴리스틱. 도메인 전문가(기획) 승인 없이 변경 금지.
- **해석 문구 AI 생성 금지** (§11): 결과 문구는 15개 블록(A용신×5, B강약×3, C조후×2, D처방×5)을 기획이 전량 작성하고 코드는 조립만 한다. 조후 평(平)이면 C 블록 생략. 블록은 DB/JSON 관리(하드코딩 금지).

## 회귀 테스트 기준 (§9)

입력: 1979-02-02(양력) 16:45, 여자, 경도보정 OFF

기대: 사주년 1978 / 원국 무오·을축·경자·갑신 / 일간 경금 / 강약 +1(중화 신강쪽) / 조후 net −2(한, need=화) / **용신 火 (both=true)** / 희신 木 · 기신 水

로직을 수정하면 이 케이스가 유지되는지 반드시 확인한다. yongsin.html 첫 진입 시 이 값이 기본 입력으로 자동 산출된다.

부적집 동일 케이스: 1979년·2월·2일 + 신(申)시 + 여자 → 결과 문구에 "물의 기운이 너무나 강한" + "게다가 사주가 차게" 포함, 용신 한자 火.

## 알려진 한계·확정된 처리 정책 (§3-3, §10)

- 절기는 수성(壽星)공식 근사(유효범위 1900–2099), 경계 ±1일 오차 가능. 상용화 시 정밀 절기 데이터로 교체하되 **방식①(절기 데이터셋만 교체)** 권장 — 나머지 명세는 그대로 유효.
- 자시는 자정 기준 일 변경(야자시/조자시 미구분). 음력 입력 미지원.
- 출생 시각은 필수 입력(없으면 진행 차단 모달), 날짜 오류는 드롭다운 제약으로 원천 차단, 실제 예외 폴백은 전역 에러 페이지 하나뿐(§10 A~E 분류 참조).

## 로직 동기화 (3곳)

엔진(§1~§6)은 세 곳에 존재한다: 명세서(소스 오브 트루스) · `yongsin.html` · 부적집 HTML의 인라인 복사본. 로직을 바꾸면 세 곳을 함께 갱신한다. 명세서는 타 언어·플랫폼 재구현용 인계 문서다.

알려진 불일치 1건: 결과 문구 조립 순서가 부적집은 피그마 기준 B→A→C→D, 명세서 §11-3은 A→B→C→D. 기획 최종안 확정 시 명세서를 갱신하기로 함(docs/2026-07-27.md §4).
